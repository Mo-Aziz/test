function authenticate(login, password){

    if(login === 'login' && password === 'password')
    {
        return' welcome  our client!!!!!';
    }else{
        return 'login process was not successful'
    }
}